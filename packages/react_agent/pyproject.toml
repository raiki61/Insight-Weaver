[project]
name = "react-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["langchain-core", "langchain-ollama", "langchain-community", "pyyaml", "pydantic", "python-dotenv"]
